---
import { services } from '../../data/site';
---

<!--
  El grid 2x2 de cards idénticas se reemplaza por una lista editorial: el
  numeral de cada servicio se vuelve gigante y fantasma (.ghost-numeral),
  el copy queda compacto y desplazado a la derecha — contraste de escala
  como estructura, no cuatro cajas iguales.
-->
<section id="servicios" class="border-cloud/10 border-t">
  <div class="px-6 py-24 sm:px-10 sm:py-32 lg:px-16">
    <div
      data-reveal-group
      class="border-cloud/10 flex flex-col gap-6 border-b pb-10 sm:flex-row sm:items-end sm:justify-between"
    >
      <div data-reveal-item>
        <p class="text-gold-500 font-mono text-xs font-medium tracking-[0.2em] uppercase">
          {services.eyebrow}
        </p>
        <h2
          class="font-display text-cloud mt-4 text-4xl leading-[1.02] font-bold tracking-tight sm:text-6xl"
        >
          {services.headline.map((line) => <span class="block">{line}</span>)}
        </h2>
      </div>
      <p data-reveal-item class="text-cloud-muted max-w-sm font-sans text-sm sm:text-right">
        {services.intro}
      </p>
    </div>

    <div data-reveal-group>
      {
        services.items.map((service) => (
          <article
            data-reveal-item
            data-hover-card
            class="group hover-glow border-cloud/10 grid grid-cols-12 items-center gap-x-4 border-b py-6 sm:py-8"
          >
            <span
              aria-hidden="true"
              class="ghost-numeral col-span-4 text-[clamp(3.5rem,9vw,7.5rem)] select-none sm:col-span-3"
            >
              {service.n.split(' ')[0]}
            </span>

            <div class="col-span-8 flex items-start justify-between gap-4 sm:col-span-9">
              <div>
                <p class="text-cloud-muted font-mono text-[11px] tracking-[0.1em]">{service.n}</p>
                <h3 class="font-display text-cloud mt-2 text-2xl font-semibold sm:text-3xl">
                  {service.title}
                </h3>
                <p class="text-cloud-muted mt-3 max-w-sm font-sans text-sm">{service.desc}</p>
              </div>
              <span
                data-hover-icon
                class="font-display text-gold-500 mt-1 inline-block shrink-0 text-2xl sm:text-3xl"
              >
                {service.icon}
              </span>
            </div>
          </article>
        ))
      }
    </div>

    <div
      data-reveal
      class="text-cloud-muted mt-10 flex flex-col gap-3 font-mono text-xs tracking-[0.06em] uppercase sm:flex-row sm:flex-wrap sm:gap-x-10"
    >
      {
        services.manifesto.map((line) => (
          <span>
            <strong class="text-cloud">{line.strong}</strong> {line.rest}
          </span>
        ))
      }
    </div>
  </div>
</section>
